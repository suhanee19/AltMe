# Rename to .env and fill with your values
MONGODB_URI=mongodb+srv://<user>:<pass>@cluster0.mongodb.net/digital_twin?retryWrites=true&w=majority
OPENAI_API_KEY=sk-...
GMAIL_CREDENTIALS_JSON=path/to/credentials.json   # or leave for manual mock
GMAIL_TOKEN_JSON=path/to/token.json               # for oauth token
